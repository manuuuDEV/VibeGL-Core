{
  "name": "vibe-gl-workspace",
  "version": "1.1.0",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "release": "pnpm build && pnpm test && changeset publish",
    "prepare": "pnpm build && husky install",
    "precommit": "node ./scripts/precommit-check.js && lint-staged"
  },
  "devDependencies": {
    "@changesets/cli": "^2.31.1",
    "@types/react": "^19.2.18",
    "@types/react-dom": "^19.2.4",
    "@types/three": "^0.185.3",
    "husky": "^9.1.7",
    "lint-staged": "^17.3.0",
    "tsup": "^8.5.1",
    "turbo": "^2.10.8",
    "typescript": "^5.9.3",
    "vitest": "^4.1.10"
  },
  "packageManager": "pnpm@8.15.1",
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "three-stdlib": "^2.36.1"
  }
}